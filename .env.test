# Test Environment Variables for ResourceFlow
# This file is for testing the migration setup
# DO NOT use these values in production

# Supabase Configuration (placeholder values for testing)
SUPABASE_URL=https://test-project-id.supabase.co
SUPABASE_ANON_KEY=test-anon-key-here

# Database Connection (placeholder for testing)
DATABASE_URL=postgresql://postgres:test-password@db.test-project-id.supabase.co:5432/postgres

# Application Environment
NODE_ENV=development

# Authentication Configuration
JWT_SECRET=test-jwt-secret-key-for-development-only
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key-for-development-only
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Session Configuration
SESSION_SECRET=test-session-secret-for-development-only
